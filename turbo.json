{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": ["DATABASE_URL"],
  "tasks": {
    "postinstall": {
      "cache": false
    },
    "build": {
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "dependsOn": ["db:generate", "^build"]
    },
    "dev": {
      "env": [
        "DATABASE_URL"
      ],
      "cache": false,
      "persistent": true
    },
    "lint": {
      "outputs": ["src/**"]
    },
    "clean": {
      "cache": false
    },
    "db:generate": {
      "inputs": ["prisma/**", "zmodel/**", "schema.zmodel"],
      "cache": true
    },
    "db:push": {
      "cache": false
    }
  }
}
